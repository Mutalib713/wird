# Wird — R8 rules.
#
# Nothing here yet. Add a rule only when a release build actually breaks, and write
# down which build broke and how, so the next person knows the rule is load-bearing
# rather than superstition.
