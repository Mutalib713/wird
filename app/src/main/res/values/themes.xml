<?xml version="1.0" encoding="utf-8"?>
<!--
  Deliberately bare. Compose draws everything, and the colour palette is Mutalib's
  choice from the design-studio picker — it is not decided here by default.
-->
<resources>
    <style name="Theme.Wird" parent="@android:style/Theme.Material.Light.NoActionBar" />
</resources>
